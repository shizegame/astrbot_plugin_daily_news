# 更新日志

今后每次版本更新均在此记录新增、修复、配置变更和验证边界。

## v2.5.0 — 2026-09-26

### 修复
- **AI资讯快报恢复可用**：60s 的 `ai-news` 持续返回空数组（`{"date":"2026-09-25","news":[]}`），而唯一备用源 `qbitai.com/feed` 已开始返回 403/502，导致整个栏目不可用。现按顺序尝试 5 个互相独立的 feed：TechCrunch AI → Ars Technica AI → The Verge AI（Atom）→ MIT Technology Review → 量子位；哪个应答就在简报里标注哪个（provider 与 tip 均写明「非原快报内容」）。
- 移除已死的聚合镜像 `60s.viki.moe`（所有路径返回 Cloudflare 403），不再每次抓取浪费一次请求和超时预算。
- feed 解析从只支持 RSS 2.0（`channel/item` + `pubDate`）扩展到 Atom（`entry`/`link@href`/`updated`）与 RSS 1.0/RDF（根级 `item` + `dc:date`），并统一 RFC822/ISO8601（含 `Z` 与时区偏移）日期解析。

### 新增
- **9 个海外内置栏目**（默认关闭，需在配置里勾选）：BBC国际新闻、卫报国际新闻、纽约时报国际新闻、半岛电视台新闻、德国之声新闻、联合国新闻、英国政府新闻（政策前沿）、STAT医学新闻（医药前沿）、自然杂志快讯（医药前沿）。全部直接取各媒体自己的公开 feed，不需要填链接；关闭时仍可用 `/news bbc` 之类单独查询。分类为「国际新闻」「医药前沿」，与简报板块标题对应。
- 新增 `news_fetch_proxy`：新闻与天气抓取共用，仅接受 `http://`/`https://` 形式（如 `http://127.0.0.1:7890`）。没有国际出口的服务器上，海外栏目需要它；格式非法会在启动时明确报错，不静默忽略；不支持 socks（aiohttp 需额外依赖）。
- 新增 `news_fetch_timeout`（默认 10 秒，3–30）：单个上游请求超时。海外 feed 在部分网络下较慢（实测 Ars Technica 4.1 秒），旧的硬编码 6 秒会误判为不可用。每个栏目的整体截止时间 = 请求超时 × 端点数（上限 120 秒），不可达主机不会拖垮整次推送。
- 新增 `image_show_links`（默认 **false**）：**图片不再显示链接**。渲染前去掉 Markdown 链接（保留标题文字）、`（URL）` 与裸 URL 行；文字消息是否带链接仍由 `include_source_links` 控制。

### 验证
- 真实探测（2026-09-26，本地沙箱）：TechCrunch AI 200/19KB、Ars Technica AI 200/77KB、The Verge AI 200/29KB、MIT Tech Review 200/102KB、BBC 200、Guardian 200、NYT 200、Al Jazeera 200、DW 200、UN News 200、GOV.UK 200、STAT News 200、Nature 200，且经 NewsClient 实抓各返回条目、日期为当天或前一天；`60s ai-news` 200 但 news 为空；qbitai 403/502。实测后剔除、避免长期「暂不可用」：NHK RSS（cat0/cat1/cat2 最新条目 2026-08-08，滞后 48 天）、WHO News RSS（最新 2026-02-25，滞后 7 个月）；未采用：CNN `rss.cnn.com`（连接失败）、机器之心 `/rss`（302 跳 HTML）、VentureBeat（429 验证页）、EurekAlert（404）、CDC（403）、Nature Medicine `nm.rss`（解析失败）。
- 本地 115 项测试通过（Atom/RDF/dc:date 解析、AI 7天与新闻 14天窗口、feed 尝试顺序、恶意/空 feed 拒绝、代理校验、图片去链接、新栏目可单独查询、schema 类型递归校验），Windows 侧同步验证。

### 网络可达性实测（2026-09-26，中国大陆出口的开发机，无代理，12秒超时）
- 可直连：TechCrunch 0.9s、The Verge 0.8s、MIT Tech Review 0.5s、Ars Technica 4.1s、UN News 1.0s、GOV.UK 0.4s、STAT News 1.0s、Nature 1.0s。
- 超时不可达：**BBC、卫报、纽约时报、半岛电视台、德国之声**（均 13 秒超时）→ 这些栏目需要配置 `news_fetch_proxy`，否则只会显示「暂不可用」。
- 同一份代码在海外出口环境下 12/12 栏目全部成功（含 BBC/卫报/纽约时报/半岛/德国之声），说明是网络出口问题而非解析问题。

### 注意
- 海外 feed 在你的 AstrBot 服务器上是否可达取决于出口/代理，仓库环境无法代为验证；不可达时该栏目会明确标注「暂不可用」，不影响其他栏目。
- 海外栏目多为英文/日文原文：开启 AI 总结时会被整理进中文板块；关闭 AI 时按原文标题直接汇总。
- 新栏目默认关闭，避免在无国际出口的服务器上产生大量「暂不可用」噪声。

## v2.4.2 — 2026-09-26

### 修复
- `_conf_schema.json` 里 `language_label_style` 的类型误写为 `str`。AstrBot 只接受 int/float/bool/string/text/list/file/object/template_list/dict（`astrbot/core/config/astrbot_config.py::_config_schema_to_default_config` 会直接抛 `TypeError: 不受支持的配置类型 str`），导致插件配置解析失败。已改为 `string`，默认值仍是 `title`，非法值按 `title` 处理。

### 新增
- 新增 `tests/test_conf_schema.py`：递归校验每个配置项（含 `object.items`）的类型都在 AstrBot 支持列表内、都有 description、default 与声明类型一致；并断言 schema 默认值能被插件读取器（`language_list`、`AISummarizer`、`WeatherClient`）直接接受、hint 中记录的数值范围与代码钳制一致、`news_sources` 全为 bool 且不全关闭。同类错误以后在测试阶段就失败，不会再发到用户端。

### 验证
- 本地 100 项测试通过；Windows 侧同步验证。真实 AstrBot WebUI 配置页加载需更新后确认。

### 注意
- 只改配置声明，运行时行为与 v2.4.1 相同；已按 v2.4.1 说明调大超时的配置无需重设。

## v2.4.1 — 2026-09-26

### 修复
- AI总结失败或关闭时的回退文档改为真正的 Markdown（`# 新闻与热榜汇总 · 日期`、`## 栏目 · 分类`、编号条目）。此前回退用的是 `【栏目名 · 分类】` 纯文本格式，这份文本又被送去翻译并渲染成图片，导致图片里没有大标题、整屏都是【】。
- 大标题不再交给模型：按语言由程序生成（每日简报 / Daily Digest / 每日簡報 / デイリーダイジェスト 等），模型只翻译正文，标题不可能被丢掉或改成括号。
- 新增结构守卫：译文标题数少于原文时，先把“只有括号的短行”（【】、[]、「」、《》、〔〕、（））按原文标题层级修复成 `#`/`##`；修复后仍不足则明确报错并跳过该语言，不再发出没有结构的整块文本。列表项与普通括号句不会被误改。
- 翻译提示词明确要求逐行保留 `#`、`-`/数字编号、`---`，禁止把标题改写成括号形式、禁止增删合并标题。

### 新增与配置
- ai_summary_timeout 默认 60→120，上限 180→300（本次 TimeoutError 回退的主要解法）。
- 新增 translate_timeout（默认120，10–300）：整篇翻译通常比总结更慢，单独配置，超时只影响该语言。
- 新增 translate_concurrency（默认2，1–4）：多语言并发翻译、按配置顺序组装，缩短总耗时。
- 新增 ai_summary_max_items（默认20，3–30）：每栏目送入模型的条目数，缩短提示词、降低超时概率。
- 新增 language_label_style（默认 title）：title=本地化大标题标识语言；bracket=保留旧的【语言】前缀行；none=不加标识。
- image_render_timeout 默认 60→90，上限 120→300（自建文转图几秒即可；公共端点排队可能超过90秒）。
- 译文长度上限 10000→20000：中文译成英文/俄文等会变长，不再被误判为“翻译过长”。

### 验证
- 新增回归测试：AI失败时图片文档含 `#`/`##` 且不含【栏目】、翻译基文档与图片同源、三种标签样式、本地化标题（en/zh-TW/ja）、括号标题修复、标题丢失显式失败、并发上限=2、超时/条目/并发钳制。本地94项测试通过，Windows侧同步验证。
- 真实模型的超时表现、译文质量与平台收图仍未在仓库测试环境中验收（全部为离线契约测试）。

### 注意
- 已安装的配置里 ai_summary_timeout、image_render_timeout 仍是旧值，更新后请在 WebUI 手动调大（建议 120–180）。
- 非中文版本的文字消息现在与图片同源，使用 Markdown 结构且不含逐条链接（缩短翻译输入）；中文文字消息仍按 include_source_links 附链接，`/news_raw` 不变。
- 若模型持续把标题写成括号且无法修复，该语言会明确失败并提示，其余语言照常发送。

## v2.4.0 — 2026-09-26

### 修复
- 限定模型只输出正文，清理首尾重复配置文案及常见问候，避免程序与模型各加一次开场白。
- 某条平台发送失败后继续发送后续语言，不把局部成功误报为全部成功。

### 新增与配置
- multilingual_enabled、news_languages：默认简体中文+英文，最多5种，可关闭或自定义顺序。
- 同一份简报生成独立语言版本：每语言一张图/独立文字消息，长译文按段落分条。
- 翻译使用AstrBot所选模型，来源链接保护、10分钟缓存、失败按语言隔离；/news_raw不翻译。

### 验证
- 新增开场去重、语言配置、翻译链接保护/缓存/超时、分语言图片/文字、长译文分条、局部失败隔离测试；本地85项测试通过。
- 真实模型翻译质量与平台收图未在仓库测试环境中验收。

### 注意
- 翻译会额外调用模型，即使关闭AI总结；不需要翻译请关闭多语言。
- 已有安装更新后新增默认中英双语；真实模型翻译和平台收图需更新重载后验收。
- 首尾去重处理文本/格式等价及常见问候，不能保证识别所有语义改写。

## v2.3.1 — 2026-09-26

### 修复
- 移除模型摘要的强制 JSON 输出/解析，解决普通 Markdown 回复触发 JSONDecodeError 的设计问题。
- 参考 SeaSmall 直接生成完整简报正文，图文共用同一结果，不再逐条JSON摘要后重组。

### 新增与配置
- 新增完整提示词 llm_prompt、板块顺序 digest_sections、开场白 digest_opening、结束语 digest_closing、正文长度 ai_summary_max_chars。
- 新增可选Open-Meteo天气和对应城市、国家筛选、预报时区、缓存、请求间隔配置；默认关闭。
- 保留旧 ai_summary_prompt 作为补充偏好，无需删除已有配置。模板提示词不要再要求输出JSON。
- 新增本更新日志；后续更新同步记录。

### 验证
- 本地与Windows侧74项测试通过；上海Open-Meteo真实预报请求成功，AI资讯/知乎/B站各返回5条。

### 边界
- 分板块按实际素材归类，不捏造缺失板块；并未新增医药/政策/GitHub专属抓取器。
- 开场白和结束语用于AI成功生成的简报；失败时仍发送明确标注的原始汇总。
- 模型与平台接口用离线契约测试验证；实际机器人模型调用和收图仍需更新重载后验收。

## v2.3.0 — 2026-09-26
- 首次接入 AstrBot 模型总结（该版结构化JSON方案已在v2.3.1替换）。
- 修复知乎/B站公开热榜接口适配；AI快报增加明确标注的量子位RSS备用源。
- 新增 /news_raw；移除失效镜像域名。

## v2.2.3 — 2026-09-26
- /news默认图片，统一使用AstrBot text_to_image，支持URL/本地路径。
- 新增 /news_image_test 与渲染/发送状态，保留文字降级。

## 更早版本（补录，日期未核实）
- v2.2.2：更新元数据指向维护者fork。
- v2.2.1：多栏目合并为一张图片/一次发送。
- v2.2.0：内置12个新闻与热榜栏目。
